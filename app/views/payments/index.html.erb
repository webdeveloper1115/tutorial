<%- model_class = Payment -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:amount) %></th>
      <th><%= model_class.human_attribute_name(:responseCode) %></th>
      <th><%= model_class.human_attribute_name(:responseMessage) %></th>
      <th><%= model_class.human_attribute_name(:xref) %></th>
      <th><%= model_class.human_attribute_name(:amountReceived) %></th>
      <th><%= model_class.human_attribute_name(:transactionID) %></th>
      <th><%= model_class.human_attribute_name(:cardNumberMask) %></th>
      <th><%= model_class.human_attribute_name(:cardTypeCode) %></th>
      <th><%= model_class.human_attribute_name(:cardType) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @payments.each do |payment| %>
      <tr>
        <td><%= link_to payment.id, payment_path(payment) %></td>
        <td><%= payment.amount %></td>
        <td><%= payment.responseCode %></td>
        <td><%= payment.responseMessage %></td>
        <td><%= payment.xref %></td>
        <td><%= payment.amountReceived %></td>
        <td><%= payment.transactionID %></td>
        <td><%= payment.cardNumberMask %></td>
        <td><%= payment.cardTypeCode %></td>
        <td><%= payment.cardType %></td>
        <td><%=l payment.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_payment_path(payment), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      payment_path(payment),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_payment_path,
            :class => 'btn btn-primary' %>
